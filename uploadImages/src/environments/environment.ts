// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
	production: false,
	firebase: {
		apiKey: "AIzaSyBoPFshJ4fTRAC1lTh2sgEHLwbMmST8RDw",
		authDomain: "mi-caja-registradora.firebaseapp.com",
		databaseURL: "https://mi-caja-registradora.firebaseio.com",
		projectId: "mi-caja-registradora",
		storageBucket: "mi-caja-registradora.appspot.com",
		messagingSenderId: "389980555154",
		appId: "1:389980555154:web:ead6c13915e2ab8c"
	}
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
